{
  "builds": [
    {
      "src": "server/server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server/server.js", "headers": { "Cache-Control": "no-cache" } }
  ]
}